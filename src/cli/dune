(executable
    (name compspec)
    (public_name compspec)
    (modes byte native)
    (modules :standard)
    (libraries ounit2 yaml "yaml.unix" "rresult" "fileutils" compspeclib) ;TODO clean libraries
)
