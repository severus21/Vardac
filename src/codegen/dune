
(library
    (name codegen)
    (public_name compspec.codegen)
    (preprocess (pps ppx_deriving.show ppx_import))
    (modules :standard)
    (libraries ounit2 yaml easy_logging "yaml.unix" "rresult" "fileutils" process core plugins)
)
